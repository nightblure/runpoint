[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "runpoint"
package = false
version = "0.3.0"
description = "CLI launcher for configured Python and Go entry points"
readme = "README.md"
requires-python = ">=3.10.0"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Operating System :: POSIX",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Typing :: Typed",
]
dependencies = [
    "debugpy",
    "psutil>=7.2.2",
    "python-dotenv",
    "rich",
    "typer",
]

[project.scripts]
runpoint = "runpoint.cli:main"

[dependency-groups]
dev = [
    { include-group = "quality" },
    "hatch",
    "pytest",
    "tomli",
]
quality = [
    "ruff",
    "ty",
]

[tool.hatch.build.targets.wheel]
packages = ["src/runpoint"]

[tool.hatch.envs.default]
python = "3.14"
installer = "uv"
dependency-groups = ["quality"]

[tool.hatch.envs.hatch-test]
extra-dependencies = ["tomli", "click"]

[[tool.hatch.envs.hatch-test.matrix]]
python = ["3.10", "3.11", "3.12", "3.13", "3.14"]

[tool.ruff]
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["ALL"]
allowed-confusables = ["Н", "б", "е", "о", "с"]
ignore = [
    "CPY001", # Copyright owner is not defined in project metadata.
    "COM812", # Conflicts with the formatter.
    "D203",   # Conflicts with D211.
    "D213",   # Conflicts with D212.
    "D100",
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"] # Plain assertions are the pytest interface.

[tool.ty.src]
include = ["src", "tests"]

[tool.ty.rules]
all = "error"

[tool.pytest.ini_options]
addopts = ["--strict-config", "--strict-markers", "-ra"]
testpaths = ["tests"]
